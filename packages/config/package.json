{
  "name": "@icon/config",
  "version": "1.0.0",
  "description": "Shared configuration for Icon Computer mobile apps",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist",
    "type-check": "tsc --noEmit"
  },
  "keywords": ["config", "react-native", "expo"],
  "author": "Trae",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^5.0.0"
  },
  "peerDependencies": {
    "react-native": "*"
  }
}